name: windows-exe

# Builds the guided builder into a single Windows executable.
#
# Separate from the release so it can be run on its own: the executable can be
# downloaded from the run's artifacts and tried on a real machine before any tag
# exists. release.yml calls this same workflow, so what gets published is what
# was tested rather than a second build of it.

on:
  workflow_dispatch:
  workflow_call:

jobs:
  build:
    # Built on Windows because that is where it runs: the EFI is almost always
    # prepared from the machine being converted, and that machine has no Python.
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install pyinstaller
      - run: pyinstaller --clean --noconfirm --distpath dist tools/pyinstaller.spec

      - name: It builds an EFI from the bundle alone
        run: |
          dist/EFIBuilderEngine/EFIBuilderEngine.exe --no-detect --answers 2,10,3,9 --out smoke/EFI
          if (-not (Test-Path smoke/EFI/OC/config.plist)) { exit 1 }

      # --check reads this runner and prints what it means, with no questions
      # to answer. A fixed answer string against whatever the runner happens to
      # be is what broke this job: a Hyper-V Xeon takes a different path through
      # the menus than a laptop does.
      - name: And detection runs without throwing
        run: dist/EFIBuilderEngine/EFIBuilderEngine.exe --check

      # SSDTTime is imported at runtime, so PyInstaller cannot see what it
      # imports. A missing module does not show until somebody answers yes to a
      # question, which is how it was found the first time. This answers it.
      - name: The vendored tools load inside the frozen build
        run: dist/EFIBuilderEngine/EFIBuilderEngine.exe --check-tools

      - name: It can dump this runner's ACPI tables and compile against them
        run: |
          # the report dumps the tables beside itself; there is no flag for it,
          # and --acpi is ambiguous with --acpi-ids and --acpi-tables
          dist/EFIBuilderEngine/EFIBuilderEngine.exe --report machine.json
          if (-not (Test-Path machine.json)) { exit 1 }
          if (-not (Test-Path ACPI)) { Write-Error 'no ACPI tables were dumped'; exit 1 }
          Get-ChildItem ACPI | Select-Object -First 8 Name
          if (-not (Test-Path ACPI/DSDT.aml)) { Write-Error 'no DSDT'; exit 1 }
          # and the tables it just dumped are the ones a build would use.
          # The machine is the fixture, not this runner: a Hyper-V Xeon answers
          # a different set of questions than a laptop does, and a fixed answer
          # string against whatever the runner happens to be is how this failed.
          dist/EFIBuilderEngine/EFIBuilderEngine.exe --machine tools/fixtures/no-hardware.json `
            --acpi-tables ACPI --answers 2,10,3,9 --out acpi-smoke/EFI
          if (-not (Test-Path acpi-smoke/EFI/OC/config.plist)) { exit 1 }
          # the automatic set, against this runner's own tables. It asks nothing,
          # which is why a script may have it, and this is the only place it can
          # be run against a real machine at all.
          if (-not (Test-Path acpi-smoke/acpi/Results)) {
            Write-Error 'the automatic SSDTs produced no Results folder'; exit 1
          }
          Get-ChildItem acpi-smoke/acpi/Results

      - uses: actions/upload-artifact@v4
        with:
          name: windows-exe
          path: dist/EFIBuilderEngine
